This template is used for myself to setup a React app quickly.

Please make sure `pnpm` is installed in your machine. Then just run `pnpm dlx degit SayaOvO/react-template {PROJECT NAME}`, then install the packages by `pnpm i`